import React, { Component } from "react";

class LogoutContext extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }
  render() {
    return <p>Should log out automatically on click instead</p>;
  }
}

export default LogoutContext;
